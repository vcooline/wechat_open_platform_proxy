<%= form_with(model: wechat_user, local: true) do |form| %>
  <% if wechat_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wechat_user.errors.count, "error") %> prohibited this wechat_user from being saved:</h2>

      <ul>
      <% wechat_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
