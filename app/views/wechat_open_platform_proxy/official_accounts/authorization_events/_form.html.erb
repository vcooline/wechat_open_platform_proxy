<%= form_with(model: official_accounts_authorization_event, local: true) do |form| %>
  <% if official_accounts_authorization_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(official_accounts_authorization_event.errors.count, "error") %> prohibited this official_accounts_authorization_event from being saved:</h2>

      <ul>
      <% official_accounts_authorization_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
